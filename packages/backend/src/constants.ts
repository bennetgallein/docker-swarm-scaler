export const ASSUME_LABEL = 'balancer.enable';
export const MAX_LABEL = 'balancer.scale.max.count';
export const MIN_LABEL = 'balancer.scale.min.count';
export const LOWER_THRESHOLD = 'balancer.scale.threshold.lower';
export const UPPER_THRESHOLD = 'balancer.scale.threshold.upper';
